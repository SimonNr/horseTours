<!DOCTYPE html>
<html>
<head>
  <title>HorseTours</title>
  <%= favicon_link_tag "/favicon.ico?1" %>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

</head>
<body>

<div id="header">
	<h3><%= link_to "HorseTours", root_url %></h3>
		<h4>
			<div id="user_nav">
				<% if user_signed_in? %>
					Signed in as <%= link_to current_user.username, edit_user_registration_path %>. Not you?
    				<%= link_to "Sign out", destroy_user_session_path, :method => :delete  %>
  				<% else %>
	    			<%= link_to "Sign up", new_user_registration_path %> or <%= link_to "sign in", new_user_session_path %>
				<% end %>
			</div>
		</h4>
</div>

<%= yield %>

</body>
</html>
