<%= render 'header' %>

<h1>Editing tour</h1>

<% if user_signed_in? %>
	<%= render 'form' %>
<% else %>
	<%= "Unable to edit tour, you are not logged in!" %>
<% end %>

<%= link_to 'Back', tours_path %>
