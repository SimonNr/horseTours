<div id="section">
	<h1>New tour</h1>

	<% if user_signed_in? %>
		<%= render 'form' %>
	<% else %>
		<%= "Unable to create tour, you are not logged in!" %>
	<% end %>
</div>
<div id="menue">
	<%= render 'shared/nav' %>

	<ul>
		<li><%= link_to 'Back', tours_path %></li>
	</ul>
</div>